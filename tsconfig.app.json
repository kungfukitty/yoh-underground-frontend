{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "jsx": "react-jsx",
    "noEmit": true // Keep this true for application code (Vite handles emit)
  },
  "include": [ "src" ],
  "exclude": [ "node_modules" ]
}